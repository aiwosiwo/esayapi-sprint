# easyapi
## 功能
- 针对Java项目自动生成API文档，包括HTML文档和Markdown文档，文档内容包括接口地址，接口描述，参数要求等。
- 简单的接口测试：自动生成接口测试参数，提供简单测试IO界面。
- 非侵入式：不需要添加额外注解，只需要为项目引入依赖即可。
- 可自定义：通过选择添加@DocIgnore注解忽略接口，不生成在文档中，通过@DocApi注解单独将接口加入API文档中。
